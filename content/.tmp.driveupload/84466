<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sitruna Knowledge Map</title>
    <style>
        body,
        html {
            margin: 0;
            padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            height: 100%;
            width: 100%;
            overflow: hidden;
            color: #1f2937;
            background-color: #f9fafb;
        }

        #app {
            width: 100%;
            height: 100%;
            display: flex;
        }

        .main-content {
            flex: 1;
            background-color: #ffffff;
            position: relative;
            overflow-y: auto;
            transition: width 0.3s ease;
            padding-top: 20px;
        }

        .main-title {
            font-size: 28px;
            font-weight: 700;
            color: #111827;
            text-align: center;
            margin-bottom: 25px;
            padding: 0 30px;
        }

        .sidebar {
            width: 320px;
            background-color: rgba(249, 250, 251, 0.98);
            border-left: 1px solid #e5e7eb;
            padding: 20px;
            overflow-y: auto;
            transition: transform 0.3s ease;
            position: fixed;
            right: 0;
            top: 0;
            bottom: 0;
            box-shadow: -3px 0 12px rgba(0, 0, 0, 0.07);
            z-index: 10;
        }

        .sidebar-collapsed {
            transform: translateX(320px);
        }

        .toggle-sidebar {
            position: fixed;
            right: 340px;
            top: 20px;
            background-color: white;
            border: 1px solid #e5e7eb;
            width: 36px;
            height: 36px;
            border-radius: 50%;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            z-index: 11;
            transition: right 0.3s ease;
        }

        .toggle-sidebar.collapsed {
            right: 20px;
        }

        .knowledge-map {
            padding: 0 30px 30px 30px;
            max-width: 1300px;
            margin: 0 auto;
        }

        .sidebar-toggle-group {
            display: flex;
            margin-bottom: 10px;
            border: 1px solid #d1d5db;
            border-radius: 8px;
            overflow: hidden;
        }

        .sidebar-toggle-btn {
            flex: 1;
            padding: 9px 10px;
            background-color: white;
            border: none;
            cursor: pointer;
            font-size: 12px;
            font-weight: 500;
            color: #4b5563;
            transition: background-color 0.2s ease, color 0.2s ease;
            text-align: center;
        }

        .sidebar-toggle-btn:not(:last-child) {
            border-right: 1px solid #d1d5db;
        }

        .sidebar-toggle-btn.active {
            background-color: #e5e7eb;
            color: #1f2937;
        }

        .sidebar-toggle-btn:hover:not(.active) {
            background-color: #f3f4f6;
        }

        .sidebar-button {
            width: 100%;
            padding: 10px 12px;
            background-color: #60a5fa;
            color: white;
            border: none;
            border-radius: 6px;
            font-size: 14px;
            font-weight: 500;
            cursor: pointer;
            transition: background-color 0.2s ease;
            margin-bottom: 20px;
        }

        .sidebar-button:hover {
            background-color: #3b82f6;
        }


        .category-section {
            margin-bottom: 25px;
        }

        .group-header {
            font-size: 20px;
            font-weight: 600;
            color: #374151;
            margin-bottom: 16px;
            padding-bottom: 10px;
            border-bottom: 2px solid #e5e7eb;
            display: flex;
            align-items: center;
            justify-content: space-between;
            cursor: pointer;
        }

        .group-header-title {
            display: flex;
            align-items: center;
        }

        .group-icon {
            margin-right: 12px;
            width: 24px;
            height: 24px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .group-toggle-icon {
            transition: transform 0.3s ease;
            color: #6b7280;
        }

        .group-toggle-icon.collapsed {
            transform: rotate(-90deg);
        }

        .items-container {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 20px;
            max-height: 2000px;
            overflow: hidden;
            transition: max-height 0.4s ease-in-out, opacity 0.3s ease-in-out;
            opacity: 1;
        }

        .items-container.collapsed {
            max-height: 0;
            opacity: 0;
            pointer-events: none;
            margin-top: -10px;
        }

        .knowledge-item {
            border: 1px solid #e5e7eb;
            border-left-width: 4px;
            border-radius: 8px;
            padding: 16px;
            background-color: white;
            box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
            position: relative;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
            cursor: pointer;
        }

        .knowledge-item:hover {
            transform: translateY(-2px);
            box-shadow: 0 3px 6px rgba(0, 0, 0, 0.08);
        }

        .knowledge-item h3 {
            margin-top: 0;
            margin-bottom: 12px;
            font-size: 16px;
            font-weight: 600;
            color: #111827;
            display: flex;
            align-items: center;
        }

        .knowledge-item-id {
            font-size: 11px;
            color: #4b5563;
            background-color: #f3f4f6;
            padding: 3px 7px;
            border-radius: 12px;
            margin-right: 8px;
            font-weight: 500;
        }

        .high-privacy {
            border-color: #ef4444;
        }

        .medium-privacy {
            border-color: #f59e0b;
        }

        .low-privacy {
            border-color: #22c55e;
        }

        .structured {
            border-style: solid;
        }

        .semi-structured {
            border-style: dashed;
        }

        .unstructured {
            border-style: dotted;
        }

        .amazon-knowledge {
            border-left-color: #3b82f6;
            background-color: rgba(59, 130, 246, 0.03);
        }

        .case-management {
            border-left-color: #a855f7;
            background-color: rgba(168, 85, 247, 0.03);
        }

        .internal-operations {
            border-left-color: #14b8a6;
            background-color: rgba(20, 184, 166, 0.03);
        }

        .client-information {
            border-left-color: #22c55e;
            background-color: rgba(34, 197, 94, 0.03);
        }

        .team-communication {
            border-left-color: #f59e0b;
            background-color: rgba(245, 158, 11, 0.03);
        }

        .system-group-item {
            border-left-color: #6b7280;
            background-color: rgba(107, 114, 128, 0.03);
        }


        .item-detail {
            display: flex;
            align-items: center;
            font-size: 13px;
            margin-bottom: 8px;
            color: #4b5563;
        }

        .item-detail svg {
            margin-right: 8px;
            flex-shrink: 0;
            color: #6b7280;
        }

        .tag-container {
            display: flex;
            flex-wrap: wrap;
            gap: 6px;
            margin-top: 12px;
        }

        .tag {
            font-size: 11px;
            padding: 3px 8px;
            border-radius: 12px;
            background-color: #e5e7eb;
            color: #374151;
            font-weight: 500;
        }

        .type-tag {
            background-color: #dbeafe;
            color: #1e40af;
        }

        /* Simple View: Hide ALL details and tags */
        .knowledge-item.simple-view .item-detail,
        .knowledge-item.simple-view .tag-container {
            display: none;
        }

        .knowledge-item.simple-view h3 {
            margin-bottom: 0;
        }


        .detail-panel {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 650px;
            max-width: 90vw;
            max-height: 85vh;
            background-color: white;
            border-radius: 12px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
            padding: 28px;
            z-index: 100;
            overflow-y: auto;
            display: none;
        }

        .detail-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 20px;
            padding-bottom: 16px;
            border-bottom: 1px solid #e5e7eb;
        }

        .detail-header h2 {
            margin: 0;
            font-size: 22px;
            font-weight: 600;
            color: #111827;
        }

        .close-button {
            background: none;
            border: none;
            font-size: 24px;
            cursor: pointer;
            color: #6b7280;
            padding: 4px;
        }

        .close-button:hover {
            color: #1f2937;
        }

        .detail-section {
            margin-bottom: 20px;
        }

        .section-title {
            font-weight: 600;
            color: #374151;
            margin-bottom: 8px;
            font-size: 16px;
        }

        .section-content {
            margin-bottom: 16px;
            font-size: 14px;
            line-height: 1.6;
            color: #374151;
        }

        .section-content ul {
            padding-left: 20px;
            margin-top: 8px;
        }

        .section-content li {
            margin-bottom: 5px;
        }

        .detail-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 16px;
            margin-bottom: 24px;
        }

        .detail-item {
            background-color: #f9fafb;
            padding: 12px 16px;
            border-radius: 8px;
            border: 1px solid #e5e7eb;
        }

        .detail-item-title {
            font-weight: 500;
            font-size: 13px;
            margin-bottom: 6px;
            color: #6b7280;
            text-transform: uppercase;
        }

        .detail-item-content {
            font-size: 15px;
            font-weight: 500;
            color: #1f2937;
        }

        .detail-tags {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            margin-top: 8px;
            margin-bottom: 16px;
        }

        .detail-tag {
            font-size: 12px;
            padding: 4px 10px;
            border-radius: 12px;
            background-color: #e5e7eb;
            color: #374151;
            font-weight: 500;
        }

        .overlay {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: rgba(0, 0, 0, 0.4);
            z-index: 90;
            display: none;
        }

        .sidebar-section {
            margin-bottom: 20px;
        }

        .sidebar-section-header {
            font-size: 16px;
            font-weight: 600;
            color: #374151;
            margin-bottom: 12px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            cursor: default;
        }

        .sidebar-section-header.clickable {
            cursor: pointer;
        }

        .filter-group {
            margin-bottom: 12px;
        }

        .filter-label {
            display: block;
            font-size: 13px;
            font-weight: 500;
            margin-bottom: 6px;
            color: #4b5563;
        }

        .filter-select,
        .filter-search {
            width: 100%;
            padding: 10px 12px;
            border-radius: 6px;
            border: 1px solid #d1d5db;
            font-size: 14px;
            background-color: white;
            color: #1f2937;
            box-sizing: border-box;
        }

        .filter-search {
            margin-bottom: 16px;
        }

        .filter-select:focus,
        .filter-search:focus {
            outline: none;
            border-color: #60a5fa;
            box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.2);
        }

        .legend-content,
        .assumptions-content {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.3s ease-out;
            font-size: 13px;
            color: #4b5563;
            line-height: 1.5;
        }

        .legend-content ul,
        .assumptions-content ul {
            padding-left: 18px;
            margin-top: 5px;
        }

        .legend-content li,
        .assumptions-content li {
            margin-bottom: 4px;
        }

        .legend-content.expanded,
        .assumptions-content.expanded {
            max-height: 800px;
        }

        .legend-group {
            margin-bottom: 16px;
        }

        .legend-title {
            font-weight: 600;
            margin-bottom: 8px;
            font-size: 14px;
            color: #374151;
        }

        .legend-item {
            display: flex;
            align-items: center;
            margin-bottom: 6px;
            font-size: 13px;
        }

        .legend-color {
            width: 16px;
            height: 16px;
            margin-right: 8px;
            border-radius: 4px;
            flex-shrink: 0;
        }

        .assumptions-content p {
            margin-bottom: 10px;
        }


        @media (max-width: 768px) {
            .sidebar {
                width: 280px;
            }

            .sidebar-collapsed {
                transform: translateX(280px);
            }

            .main-title {
                font-size: 24px;
            }

            .knowledge-map {
                padding: 0 20px 20px 20px;
            }

            .items-container {
                grid-template-columns: 1fr;
            }

            .detail-panel {
                width: 95vw;
                max-height: 90vh;
            }
        }
    </style>
</head>

<body>
    <div id="app">
        <div class="main-content">
            <h1 class="main-title">Sitruna Interactive Knowledge Map</h1>
            <div class="knowledge-map" id="knowledge-map"></div>
        </div>

        <button id="toggle-sidebar" class="toggle-sidebar">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
                stroke-linecap="round" stroke-linejoin="round">
                <path d="M12 3c.132 0 .263 0 .393 0a7.5 7.5 0 0 0 7.92 12.446a9 9 0 1 1 -8.313 -12.454z"></path>
            </svg>
        </button>

        <div id="sidebar" class="sidebar">
            <h1 style="font-size: 20px; font-weight: 600; margin-bottom: 16px; color: #111827;">Knowledge Navigator
            </h1>

            <input type="text" id="search-input" class="filter-search" placeholder="Search knowledge items...">

            <div class="sidebar-section">
                <label class="filter-label" style="margin-bottom: 8px;">Display Mode</label>
                <div class="sidebar-toggle-group">
                    <button id="simple-view-btn" class="sidebar-toggle-btn active">Simple</button>
                    <button id="detailed-view-btn" class="sidebar-toggle-btn">Detailed</button>
                </div>
            </div>

            <div class="sidebar-section">
                <label class="filter-label" style="margin-bottom: 8px;">Group By</label>
                <div class="sidebar-toggle-group">
                    <button id="group-by-category-btn" class="sidebar-toggle-btn active">Category</button>
                    <button id="group-by-system-btn" class="sidebar-toggle-btn">System/Tool</button>
                </div>
            </div>

            <button id="reset-filters-btn" class="sidebar-button">Reset All</button>

            <div class="sidebar-section">
                <div class="sidebar-section-header">
                    <span>Filters</span>
                </div>
                <div class="filter-group">
                    <label for="category-filter" class="filter-label">Knowledge Category</label>
                    <select id="category-filter" class="filter-select">
                        <option value="">All Categories</option>
                        <option value="AMAZON_KNOWLEDGE">Amazon Knowledge</option>
                        <option value="CASE_MANAGEMENT">Case Management</option>
                        <option value="INTERNAL_OPERATIONS">Internal Operations</option>
                        <option value="CLIENT_INFORMATION">Client Information</option>
                        <option value="TEAM_COMMUNICATION">Team Communication</option>
                    </select>
                </div>
                <div class="filter-group">
                    <label for="system-filter" class="filter-label">System/Tool</label>
                    <select id="system-filter" class="filter-select">
                        <option value="">All Systems</option>
                        <option value="CLICKUP">ClickUp</option>
                        <option value="GOOGLE_SITES">Google Sites</option>
                        <option value="SLACK">Slack</option>
                        <option value="GOOGLE_SHEETS">Google Sheets</option>
                        <option value="FRONT">Front</option>
                        <option value="GOOGLE_DRIVE">Google Drive</option>
                        <option value="BIG_QUERY">Big Query</option>
                        <option value="CHATGPT">ChatGPT</option>
                        <option value="AMAZON_SC">Amazon Seller Central</option>
                    </select>
                </div>
                <div class="filter-group">
                    <label for="type-filter" class="filter-label">Content Type</label>
                    <select id="type-filter" class="filter-select">
                        <option value="">All Types</option>
                        <option value="First-Party">First-Party</option>
                        <option value="Third-Party">Third-Party</option>
                        <option value="Client-Owned">Client-Owned</option>
                    </select>
                </div>
                <div class="filter-group">
                    <label for="privacy-filter" class="filter-label">Privacy Level</label>
                    <select id="privacy-filter" class="filter-select">
                        <option value="">All Levels</option>
                        <option value="High">High</option>
                        <option value="Medium">Medium</option>
                        <option value="Low">Low</option>
                    </select>
                </div>
                <div class="filter-group">
                    <label for="structure-filter" class="filter-label">Data Structure</label>
                    <select id="structure-filter" class="filter-select">
                        <option value="">All Structures</option>
                        <option value="Structured">Structured</option>
                        <option value="Semi-structured">Semi-structured</option>
                        <option value="Unstructured">Unstructured</option>
                    </select>
                </div>
                <div class="filter-group">
                    <label for="ai-filter" class="filter-label">AI Integration</label>
                    <select id="ai-filter" class="filter-select">
                        <option value="">Any Priority</option>
                        <option value="High">High Priority</option>
                        <option value="Medium">Medium Priority</option>
                        <option value="Low">Low Priority</option>
                    </select>
                </div>
            </div>

            <div class="sidebar-section">
                <div class="sidebar-section-header clickable" id="legend-toggle">
                    <span>Legend</span>
                    <svg id="legend-icon" class="group-toggle-icon" width="20" height="20" viewBox="0 0 24 24"
                        fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
                        stroke-linejoin="round">
                        <polyline points="6 9 12 15 18 9"></polyline>
                    </svg>
                </div>
                <div class="legend-content" id="legend-content">
                    <div class="legend-group">
                        <h4 class="legend-title">Knowledge Categories (Left Border & Tint)</h4>
                        <div class="legend-item">
                            <div class="legend-color"
                                style="background-color: rgba(59, 130, 246, 0.1); border-left: 4px solid #3b82f6;">
                            </div>
                            Amazon Knowledge
                        </div>
                        <div class="legend-item">
                            <div class="legend-color"
                                style="background-color: rgba(168, 85, 247, 0.1); border-left: 4px solid #a855f7;">
                            </div>
                            Case Management
                        </div>
                        <div class="legend-item">
                            <div class="legend-color"
                                style="background-color: rgba(20, 184, 166, 0.1); border-left: 4px solid #14b8a6;">
                            </div>
                            Internal Operations
                        </div>
                        <div class="legend-item">
                            <div class="legend-color"
                                style="background-color: rgba(34, 197, 94, 0.1); border-left: 4px solid #22c55e;"></div>
                            Client Information
                        </div>
                        <div class="legend-item">
                            <div class="legend-color"
                                style="background-color: rgba(245, 158, 11, 0.1); border-left: 4px solid #f59e0b;">
                            </div>
                            Team Communication
                        </div>
                    </div>
                    <div class="legend-group">
                        <h4 class="legend-title">Systems/Tools (Left Border & Tint - when grouped)</h4>
                        <div class="legend-item">
                            <div class="legend-color"
                                style="background-color: rgba(107, 114, 128, 0.1); border-left: 4px solid #6b7280;">
                            </div>
                            Generic System/Tool
                        </div>
                    </div>
                    <div class="legend-group">
                        <h4 class="legend-title">Privacy Level (Main Border Color)</h4>
                        <div class="legend-item">
                            <div class="legend-color" style="border: 2px solid #ef4444;"></div>High
                        </div>
                        <div class="legend-item">
                            <div class="legend-color" style="border: 2px solid #f59e0b;"></div>Medium
                        </div>
                        <div class="legend-item">
                            <div class="legend-color" style="border: 2px solid #22c55e;"></div>Low
                        </div>
                    </div>
                    <div class="legend-group">
                        <h4 class="legend-title">Data Structure (Main Border Style)</h4>
                        <div class="legend-item">
                            <div class="legend-color" style="border: 2px solid #6b7280;"></div>Structured
                        </div>
                        <div class="legend-item">
                            <div class="legend-color" style="border: 2px dashed #6b7280;"></div>Semi-structured
                        </div>
                        <div class="legend-item">
                            <div class="legend-color" style="border: 2px dotted #6b7280;"></div>Unstructured
                        </div>
                    </div>
                </div>
            </div>

            <div class="sidebar-section">
                <div class="sidebar-section-header clickable" id="assumptions-toggle">
                    <span>Assumptions & Enrichments</span>
                    <svg id="assumptions-icon" class="group-toggle-icon" width="20" height="20" viewBox="0 0 24 24"
                        fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
                        stroke-linejoin="round">
                        <polyline points="6 9 12 15 18 9"></polyline>
                    </svg>
                </div>
                <div class="assumptions-content" id="assumptions-content">
                    <p>This map visualizes the provided knowledge data. Some interpretations are made for clarity:</p>
                    <ul>
                        <li><strong>Potential Challenges:</strong> Derived by analyzing 'Implementation Notes' for
                            keywords (e.g., "challenge," "complex"), and considering attributes like 'Data Structure'
                            (Unstructured implies organizational needs), 'Privacy Sensitivity' (High implies careful
                            handling), and a high number of 'Systems/Tools'.</li>
                        <li><strong>AI Integration Workflow:</strong> The 'AI Integration Priority' field is used. Items
                            with clear, actionable steps (often indicated by '→') or explicit 'High' mentions are
                            highlighted in the detail panel to signify potential for AI-driven enhancements.</li>
                    </ul>
                </div>
            </div>
        </div>

        <div class="overlay" id="overlay"></div>
        <div class="detail-panel" id="detail-panel">
            <div class="detail-header">
                <h2 id="detail-title">Item Details</h2>
                <button class="close-button" id="close-detail">×</button>
            </div>
            <div id="detail-content"></div>
        </div>
    </div>

    <script>
        const knowledgeItems = [
            { id: '1.1', label: 'Marketplace Policies', category: 'AMAZON_KNOWLEDGE', systems: ['CLICKUP', 'GOOGLE_SITES'], team: 'All Staff', type: ['First-Party', 'Third-Party'], structure: 'Structured', privacy: 'High', implementationNotes: 'Critical for case compliance and decision-making; Includes VAT requirements', primaryUsers: 'Amazon Knowledge > Marketplace Policies', aiIntegrationPriority: 'Amazon policy changes → Documentation → Team implementation → Case handling', details: 'VAT registration requirements, Country-specific tax rules, HFSS Exemption Application, Brand protection informations, Account configuration, User permissions' },
            { id: '1.2', label: 'Listing Management', category: 'AMAZON_KNOWLEDGE', systems: ['CLICKUP', 'GOOGLE_SITES'], team: 'All Staff', type: ['First-Party', 'Third-Party'], structure: 'Semi-structured', privacy: 'Medium', implementationNotes: 'Core knowledge for product listings; Includes creation procedures', primaryUsers: 'Amazon Knowledge > Listing Management', aiIntegrationPriority: 'Team expertise → Documentation → Client implementation', details: 'Listing creation procedures, Buyer-seller messaging, Review management' },
            { id: '1.3', label: 'Inventory & Fulfillment', category: 'AMAZON_KNOWLEDGE', systems: ['CLICKUP', 'GOOGLE_SITES'], team: 'All Staff', type: ['First-Party', 'Third-Party'], structure: 'Structured', privacy: 'Medium', implementationNotes: 'Essential for inventory operations; Includes shipment plans', primaryUsers: 'Amazon Knowledge > Inventory & Fulfillment', aiIntegrationPriority: 'Team expertise → Documentation → Client implementation', details: 'Create FBA Shipment Plan, Shipping & Warehouse Partners, Inbound units management, FBM SKU management' },
            { id: '1.4', label: 'Promotions & Pricing', category: 'AMAZON_KNOWLEDGE', systems: ['CLICKUP', 'GOOGLE_SITES'], team: 'All Staff', type: ['First-Party', 'Third-Party'], structure: 'Semi-structured', privacy: 'Medium', implementationNotes: 'Strategy and implementation knowledge; Includes pricing strategies', primaryUsers: 'Amazon Knowledge > Promotions & Pricing', aiIntegrationPriority: 'Team expertise → Documentation → Client implementation', details: 'Pricing strategy, Promotional campaigns, Lightning deals' },
            { id: '1.5', label: 'International Selling', category: 'AMAZON_KNOWLEDGE', systems: ['CLICKUP', 'GOOGLE_SITES'], team: 'All Staff', type: ['First-Party', 'Third-Party'], structure: 'Structured', privacy: 'Medium', implementationNotes: 'Cross-border selling requirements; Includes market requirements', primaryUsers: 'Amazon Knowledge > International Selling', aiIntegrationPriority: 'Regulatory requirements → Documentation → Client implementation', details: 'Market entry requirements, Cross-border logistics, Translation services, Cross-border logistics' },
            { id: '2.1', label: 'Case Templates', category: 'CASE_MANAGEMENT', systems: ['CLICKUP', 'CHATGPT', 'AMAZON_SC'], team: 'All Staff', type: ['First-Party'], structure: 'Structured', privacy: 'High', implementationNotes: 'High-value standardized formats; Includes suspension appeals', primaryUsers: 'Case Management > Case Templates', aiIntegrationPriority: 'Past cases → Templates → Appeal submission', details: 'Account suspension templates, Listing reinstatement processes, Response templates by case type' },
            { id: '2.2', label: 'Case Processes', category: 'CASE_MANAGEMENT', systems: ['CLICKUP'], team: 'All Staff', type: ['First-Party'], structure: 'Structured', privacy: 'High', implementationNotes: 'Standard operating procedures for cases; Includes tracking methods', primaryUsers: 'Case Management > Case Processes', aiIntegrationPriority: 'Team expertise → Documentation → Case handling', details: 'Case tracking methodology, Escalation procedures' },
            { id: '2.3', label: 'Appeals & Compliance', category: 'CASE_MANAGEMENT', systems: ['CLICKUP', 'GOOGLE_SITES'], team: 'All Staff', type: ['First-Party'], structure: 'Structured', privacy: 'High', implementationNotes: 'Critical for account health issues; Includes ASIN restoration', primaryUsers: 'Case Management > Appeals & Compliance', aiIntegrationPriority: 'Amazon policies → Documentation → Appeal submission', details: 'ASIN restoration guidelines, Registration process' },
            { id: '3.1', label: 'Company Structure', category: 'INTERNAL_OPERATIONS', systems: ['CLICKUP'], team: 'All Staff', type: ['First-Party'], structure: 'Structured', privacy: 'Low', implementationNotes: 'Organizational framework; Includes org charts', primaryUsers: 'Internal Operations > Company Structure', aiIntegrationPriority: 'Management decisions → Documentation → Team reference', details: 'Org Chart, Company Policies, VAT Number + Companies House, Certificate of Engagement/Employment, Team directory' },
            { id: '3.2', label: 'Operational Procedures', category: 'INTERNAL_OPERATIONS', systems: ['CLICKUP'], team: 'All Staff', type: ['First-Party'], structure: 'Semi-structured', privacy: 'Medium', implementationNotes: 'Standard business processes; Includes standard processes', primaryUsers: 'Internal Operations > Operational Procedures', aiIntegrationPriority: 'Team expertise → Documentation → Team implementation', details: 'Standard process templates, Documentation templates, Travel Booking, Cross-functional processes' },
            { id: '3.3', label: 'Team Development', category: 'INTERNAL_OPERATIONS', systems: ['CLICKUP', 'GOOGLE_SITES'], team: 'All Staff', type: ['First-Party'], structure: 'Semi-structured', privacy: 'Medium', implementationNotes: 'Staff training and growth; Includes training', primaryUsers: 'Internal Operations > Team Development', aiIntegrationPriority: 'Expert knowledge → Training materials → Team capabilities', details: 'Training, New Starter Onboarding, Sitruna University training, Sitruna Best Practices Doc, Onboarding checklist' },
            { id: '3.4', label: 'Technical Systems', category: 'INTERNAL_OPERATIONS', systems: ['CLICKUP'], team: 'All Staff', type: ['First-Party'], structure: 'Semi-structured', privacy: 'Low', implementationNotes: 'Technology infrastructure; Includes internal tools', primaryUsers: 'Internal Operations > Technical Systems', aiIntegrationPriority: 'Technical setup → Documentation → Team usage', details: 'Internal tools, Third-party applications, Access management' },
            { id: '4.1', label: 'Client Accounts', category: 'CLIENT_INFORMATION', systems: ['CLICKUP', 'SLACK'], team: 'All Staff', type: ['First-Party'], structure: 'Semi-structured', privacy: 'Medium', implementationNotes: 'Client-specific workspaces; Includes all active client accounts (Botivo', primaryUsers: 'Client Information > Client Accounts', aiIntegrationPriority: 'Client information → Task creation → Client servicing', details: 'Active clients, Historical client information' },
            { id: '4.2', label: 'Client Management', category: 'CLIENT_INFORMATION', systems: ['CLICKUP', 'GOOGLE_SHEETS', 'SLACK', 'FRONT'], team: 'All Staff', type: ['First-Party'], structure: 'Structured', privacy: 'Medium', implementationNotes: 'Client relationship data; Includes overview data', primaryUsers: 'Client Information > Client Management', aiIntegrationPriority: 'Client onboarding → Data entry → Team reference', details: 'Client overview data, Team assignments, Service package details, Account setup procedures, Initial optimization tasks' },
            { id: '4.3', label: 'Client Assets', category: 'CLIENT_INFORMATION', systems: ['GOOGLE_DRIVE', 'GOOGLE_SHEETS'], team: 'All Staff', type: ['Client-Owned', 'First-Party'], structure: 'Unstructured', privacy: 'Low', implementationNotes: 'Visual and brand materials; Includes brand materials', primaryUsers: 'Client Information > Client Assets', aiIntegrationPriority: 'Client provision → Storage → Implementation', details: 'Brand materials, Product images, Marketing collateral, Documentation and guides' },
            { id: '4.4', label: 'Client Performance', category: 'CLIENT_INFORMATION', systems: ['CLICKUP', 'BIG_QUERY', 'GOOGLE_SHEETS'], team: 'All Staff', type: ['First-Party'], structure: 'Structured', privacy: 'Low', implementationNotes: 'Success metrics and analytics; Includes metrics', primaryUsers: 'Client Information > Client Performance', aiIntegrationPriority: 'Data collection → Analysis → Client reporting', details: 'Client performance data' },
            { id: '5.1', label: 'Client Communication', category: 'TEAM_COMMUNICATION', systems: ['FRONT', 'SLACK'], team: 'All Staff', type: ['First-Party'], structure: 'Unstructured', privacy: 'Medium', implementationNotes: 'External correspondence; Includes client correspondence', primaryUsers: 'Account Team', aiIntegrationPriority: 'Client emails → Response → Resolution', details: 'Client correspondence, Client-specific channels' },
            { id: '5.2', label: 'Internal Collaboration', category: 'TEAM_COMMUNICATION', systems: ['SLACK', 'FRONT', 'CLICKUP'], team: 'All Staff', type: ['First-Party'], structure: 'Unstructured', privacy: 'Medium', implementationNotes: 'Team discussions; Includes team discussions', primaryUsers: 'Team Communication > Internal Collaboration', aiIntegrationPriority: 'Team questions → Discussion → Resolution', details: 'Team communication, Internal discussions, Department-specific knowledge, Specialized projects, Short-term engagements' },
            { id: '5.3', label: 'Knowledge Sharing', category: 'TEAM_COMMUNICATION', systems: ['SLACK', 'CLICKUP', 'FRONT'], team: 'All Staff', type: ['Third-Party', 'First-Party'], structure: 'Unstructured', privacy: 'Medium', implementationNotes: 'Information distribution; Includes updates', primaryUsers: 'Team Communication > Knowledge Sharing', aiIntegrationPriority: 'Discoveries → Communication → Team knowledge', details: 'Knowledge sharing, Amazon updates, Reference materials' }
        ];

        const categoryLabels = { 'AMAZON_KNOWLEDGE': 'Amazon Knowledge', 'CASE_MANAGEMENT': 'Case Management', 'INTERNAL_OPERATIONS': 'Internal Operations', 'CLIENT_INFORMATION': 'Client Information', 'TEAM_COMMUNICATION': 'Team Communication' };
        const systemLabels = { 'CLICKUP': 'ClickUp', 'GOOGLE_SITES': 'Google Sites', 'SLACK': 'Slack', 'GOOGLE_SHEETS': 'Google Sheets', 'FRONT': 'Front', 'GOOGLE_DRIVE': 'Google Drive', 'BIG_QUERY': 'Big Query', 'CHATGPT': 'ChatGPT', 'AMAZON_SC': 'Amazon Seller Central' };

        const knowledgeMapEl = document.getElementById('knowledge-map');
        const detailPanelEl = document.getElementById('detail-panel');
        const closeDetailBtn = document.getElementById('close-detail');
        const detailTitleEl = document.getElementById('detail-title');
        const detailContentEl = document.getElementById('detail-content');
        const toggleSidebarBtn = document.getElementById('toggle-sidebar');
        const sidebarEl = document.getElementById('sidebar');
        const overlayEl = document.getElementById('overlay');

        const simpleViewBtn = document.getElementById('simple-view-btn');
        const detailedViewBtn = document.getElementById('detailed-view-btn');
        const groupByCategoryBtn = document.getElementById('group-by-category-btn');
        const groupBySystemBtn = document.getElementById('group-by-system-btn');
        const resetFiltersBtn = document.getElementById('reset-filters-btn');

        const legendToggle = document.getElementById('legend-toggle');
        const legendContent = document.getElementById('legend-content');
        const legendIcon = document.getElementById('legend-icon');
        const assumptionsToggle = document.getElementById('assumptions-toggle');
        const assumptionsContent = document.getElementById('assumptions-content');
        const assumptionsIcon = document.getElementById('assumptions-icon');

        const searchInput = document.getElementById('search-input');
        const categoryFilter = document.getElementById('category-filter');
        const systemFilter = document.getElementById('system-filter');
        const typeFilter = document.getElementById('type-filter');
        const privacyFilter = document.getElementById('privacy-filter');
        const structureFilter = document.getElementById('structure-filter');
        const aiFilter = document.getElementById('ai-filter');

        let isSimpleView = true; // Default to simple view
        let currentGrouping = 'category'; // Default grouping

        // --- Event Listeners ---
        simpleViewBtn.addEventListener('click', () => {
            if (!isSimpleView) {
                isSimpleView = true;
                simpleViewBtn.classList.add('active');
                detailedViewBtn.classList.remove('active');
                filterItems();
            }
        });

        detailedViewBtn.addEventListener('click', () => {
            if (isSimpleView) {
                isSimpleView = false;
                detailedViewBtn.classList.add('active');
                simpleViewBtn.classList.remove('active');
                filterItems();
            }
        });

        groupByCategoryBtn.addEventListener('click', () => {
            if (currentGrouping !== 'category') {
                currentGrouping = 'category';
                groupByCategoryBtn.classList.add('active');
                groupBySystemBtn.classList.remove('active');
                filterItems();
            }
        });

        groupBySystemBtn.addEventListener('click', () => {
            if (currentGrouping !== 'system') {
                currentGrouping = 'system';
                groupBySystemBtn.classList.add('active');
                groupByCategoryBtn.classList.remove('active');
                filterItems();
            }
        });

        resetFiltersBtn.addEventListener('click', () => {
            searchInput.value = '';
            categoryFilter.value = '';
            systemFilter.value = '';
            typeFilter.value = '';
            privacyFilter.value = '';
            structureFilter.value = '';
            aiFilter.value = '';

            isSimpleView = true;
            simpleViewBtn.classList.add('active');
            detailedViewBtn.classList.remove('active');

            currentGrouping = 'category';
            groupByCategoryBtn.classList.add('active');
            groupBySystemBtn.classList.remove('active');

            filterItems();
        });

        toggleSidebarBtn.addEventListener('click', () => {
            sidebarEl.classList.toggle('sidebar-collapsed');
            toggleSidebarBtn.classList.toggle('collapsed');
        });

        legendToggle.addEventListener('click', () => {
            legendContent.classList.toggle('expanded');
            legendIcon.classList.toggle('collapsed');
        });

        assumptionsToggle.addEventListener('click', () => {
            assumptionsContent.classList.toggle('expanded');
            assumptionsIcon.classList.toggle('collapsed');
        });

        closeDetailBtn.addEventListener('click', () => { detailPanelEl.style.display = 'none'; overlayEl.style.display = 'none'; });
        overlayEl.addEventListener('click', () => { detailPanelEl.style.display = 'none'; overlayEl.style.display = 'none'; });

        [searchInput, categoryFilter, systemFilter, typeFilter, privacyFilter, structureFilter, aiFilter].forEach(el => {
            el.addEventListener('input', filterItems);
            if (el.tagName === 'SELECT') el.addEventListener('change', filterItems);
        });


        // --- Helper Functions ---
        function getCategoryClass(category) {
            const mapping = { 'AMAZON_KNOWLEDGE': 'amazon-knowledge', 'CASE_MANAGEMENT': 'case-management', 'INTERNAL_OPERATIONS': 'internal-operations', 'CLIENT_INFORMATION': 'client-information', 'TEAM_COMMUNICATION': 'team-communication' };
            return mapping[category] || 'system-group-item';
        }

        function getGroupDisplayIcon(groupKey, groupingType) {
            if (groupingType === 'category') return getCategoryIcon(groupKey);
            if (groupingType === 'system') return getSystemIcon(groupKey);
            return '';
        }

        function getCategoryIcon(category) {
            const icons = {
                AMAZON_KNOWLEDGE: '<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#3b82f6" stroke-width="2"><path d="M21 8v8a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/><polyline points="3 10 12 17 21 10"/></svg>',
                CASE_MANAGEMENT: '<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#a855f7" stroke-width="2"><path d="M22 12h-4l-3 9L9 3l-3 9H2"/></svg>',
                INTERNAL_OPERATIONS: '<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#14b8a6" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>',
                CLIENT_INFORMATION: '<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#22c55e" stroke-width="2"><path d="M16 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="8.5" cy="7" r="4"/><line x1="20" y1="8" x2="20" y2="14"/><line x1="23" y1="11" x2="17" y2="11"/></svg>',
                TEAM_COMMUNICATION: '<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#f59e0b" stroke-width="2"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>'
            };
            return icons[category] || '<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#6b7280" stroke-width="2"><circle cx="12" cy="12" r="3"></circle><line x1="12" y1="2" x2="12" y2="5"></line><line x1="12" y1="19" x2="12" y2="22"></line><line x1="2" y1="12" x2="5" y2="12"></line><line x1="19" y1="12" x2="22" y2="12"></line><line x1="4.93" y1="4.93" x2="7.05" y2="7.05"></line><line x1="16.95" y1="16.95" x2="19.07" y2="19.07"></line><line x1="4.93" y1="19.07" x2="7.05" y2="16.95"></line><line x1="16.95" y1="7.05" x2="19.07" y2="4.93"></line></svg>';
        }

        function getSystemIcon(system) {
            const genericCogIcon = '<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#6b7280" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19.13 16.63a2.34 2.34 0 0 0-3.29 0l-1.83 1.83a2.34 2.34 0 0 0 0 3.29l1.83 1.83a2.34 2.34 0 0 0 3.29 0l1.83-1.83a2.34 2.34 0 0 0 0-3.29Z"/><path d="m12 12-1.83 1.83a2.34 2.34 0 0 0 0 3.29l1.83 1.83a2.34 2.34 0 0 0 3.29 0l1.83-1.83a2.34 2.34 0 0 0 0-3.29Z"/><path d="M4.87 7.37a2.34 2.34 0 0 0-3.29 0L-.23 9.2a2.34 2.34 0 0 0 0 3.29l1.83 1.83a2.34 2.34 0 0 0 3.29 0l1.83-1.83a2.34 2.34 0 0 0 0-3.29Z"/></svg>';
            return genericCogIcon;
        }


        function isHighAIPriority(priority) { return priority && (priority.includes('→') || priority.toLowerCase().includes('high') || priority.toLowerCase().includes('priority')); }
        function extractChallenges(item) {
            const challenges = [];
            if (item.implementationNotes) {
                const parts = item.implementationNotes.split(';');
                parts.forEach(part => { if (['challenge', 'difficult', 'issue', 'problem', 'complex'].some(kw => part.toLowerCase().includes(kw))) challenges.push(part.trim()); });
            }
            if (item.structure === 'Unstructured') challenges.push('Unstructured format may pose organization challenges.');
            if (item.privacy === 'High') challenges.push('High privacy sensitivity requires careful handling.');
            if (item.systems && item.systems.length > 2) challenges.push('Information distributed across multiple systems.');
            return challenges.length > 0 ? challenges : ['No significant challenges identified from notes.'];
        }

        // --- Main Rendering Logic ---
        function renderKnowledgeMap(itemsToRender) {
            knowledgeMapEl.innerHTML = '';
            const groupedItems = {};
            let groupLabelMap, groupIconFunc, groupClassFunc;

            if (currentGrouping === 'category') {
                groupLabelMap = categoryLabels;
                groupIconFunc = getCategoryIcon;
                groupClassFunc = getCategoryClass;
                itemsToRender.forEach(item => {
                    const key = item.category;
                    if (!groupedItems[key]) groupedItems[key] = [];
                    groupedItems[key].push(item);
                });
            } else { // currentGrouping === 'system'
                groupLabelMap = systemLabels;
                groupIconFunc = getSystemIcon;
                groupClassFunc = () => 'system-group-item';
                itemsToRender.forEach(item => {
                    item.systems.forEach(systemKey => {
                        if (!groupedItems[systemKey]) groupedItems[systemKey] = [];
                        if (!groupedItems[systemKey].find(i => i.id === item.id)) {
                            groupedItems[systemKey].push(item);
                        }
                    });
                });
            }

            const sortedGroupKeys = Object.keys(groupedItems).sort((a, b) => (groupLabelMap[a] || a).localeCompare(groupLabelMap[b] || b));

            for (const groupKey of sortedGroupKeys) {
                const itemsInGroup = groupedItems[groupKey];
                const groupSection = document.createElement('div');
                groupSection.className = 'category-section';

                const groupHeader = document.createElement('h2');
                groupHeader.className = 'group-header';

                const titleDiv = document.createElement('div');
                titleDiv.className = 'group-header-title';
                titleDiv.innerHTML = `<div class="group-icon">${groupIconFunc(groupKey, currentGrouping)}</div>${groupLabelMap[groupKey] || groupKey}`;

                const toggleIcon = document.createElement('span');
                toggleIcon.className = 'group-toggle-icon';
                toggleIcon.innerHTML = `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"></polyline></svg>`;

                groupHeader.appendChild(titleDiv);
                groupHeader.appendChild(toggleIcon);

                const itemsContainer = document.createElement('div');
                itemsContainer.className = 'items-container';

                itemsInGroup.sort((a, b) => parseFloat(a.id) - parseFloat(b.id)).forEach(item => itemsContainer.appendChild(createKnowledgeItemElement(item, groupClassFunc(item.category)))); // Pass category class for coloring

                groupHeader.addEventListener('click', () => {
                    itemsContainer.classList.toggle('collapsed');
                    toggleIcon.classList.toggle('collapsed');
                });

                groupSection.appendChild(groupHeader);
                groupSection.appendChild(itemsContainer);
                knowledgeMapEl.appendChild(groupSection);
            }

            if (itemsToRender.length === 0 && Object.keys(groupedItems).length === 0) {
                const noResults = document.createElement('div');
                noResults.style.cssText = 'padding: 3rem; text-align: center; color: #6b7280; font-weight: 500;';
                noResults.textContent = 'No knowledge items match your criteria.';
                knowledgeMapEl.appendChild(noResults);
            }
        }

        function createKnowledgeItemElement(item, itemSpecificClass) {
            const itemEl = document.createElement('div');
            itemEl.className = `knowledge-item ${itemSpecificClass || getCategoryClass(item.category)}`;
            if (isSimpleView) itemEl.classList.add('simple-view');

            if (item.privacy === 'High') itemEl.classList.add('high-privacy');
            else if (item.privacy === 'Medium') itemEl.classList.add('medium-privacy');
            else if (item.privacy === 'Low') itemEl.classList.add('low-privacy');

            if (item.structure === 'Structured') itemEl.classList.add('structured');
            else if (item.structure === 'Semi-structured') itemEl.classList.add('semi-structured');
            else if (item.structure === 'Unstructured') itemEl.classList.add('unstructured');

            const title = document.createElement('h3');
            title.innerHTML = `<span class="knowledge-item-id">${item.id}</span>${item.label}`;
            itemEl.appendChild(title);

            // These details will be hidden by CSS if .simple-view is active on itemEl
            const privacyDetail = document.createElement('div');
            privacyDetail.className = 'item-detail';
            privacyDetail.innerHTML = `<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>Privacy: ${item.privacy}`;
            itemEl.appendChild(privacyDetail);

            const structureDetail = document.createElement('div');
            structureDetail.className = 'item-detail';
            structureDetail.innerHTML = `<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><line x1="3" y1="9" x2="21" y2="9"/></svg>Structure: ${item.structure}`;
            itemEl.appendChild(structureDetail);

            if (item.implementationNotes) {
                const implementation = document.createElement('div');
                implementation.className = 'item-detail';
                implementation.innerHTML = `<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/></svg>`;
                const noteSnippet = item.implementationNotes.split(';')[0];
                implementation.innerHTML += `Notes: ${noteSnippet.substring(0, 40)}${noteSnippet.length > 40 ? '...' : ''}`;
                itemEl.appendChild(implementation);
            }

            const tagContainer = document.createElement('div');
            tagContainer.className = 'tag-container';
            item.systems.forEach(system => {
                const tag = document.createElement('span');
                tag.className = 'tag';
                tag.textContent = systemLabels[system] || system;
                tagContainer.appendChild(tag);
            });
            if (item.type && item.type.length > 0) {
                item.type.forEach(type => {
                    const tag = document.createElement('span');
                    tag.className = 'tag type-tag';
                    tag.textContent = type;
                    tagContainer.appendChild(tag);
                });
            }
            itemEl.appendChild(tagContainer);

            itemEl.addEventListener('click', () => showItemDetails(item));
            return itemEl;
        }

        function showItemDetails(item) {
            detailTitleEl.textContent = `${item.id} - ${item.label}`;
            let content = `<div class="detail-grid">
                <div class="detail-item"><div class="detail-item-title">Category</div><div class="detail-item-content">${categoryLabels[item.category] || item.category}</div></div>
                <div class="detail-item"><div class="detail-item-title">Privacy Level</div><div class="detail-item-content">${item.privacy}</div></div>
                <div class="detail-item"><div class="detail-item-title">Data Structure</div><div class="detail-item-content">${item.structure}</div></div>
                <div class="detail-item"><div class="detail-item-title">Team</div><div class="detail-item-content">${item.team}</div></div>
            </div>`;
            content += `<div class="section-title">Systems/Tools</div><div class="detail-tags">${item.systems.map(s => `<span class="detail-tag">${systemLabels[s] || s}</span>`).join('')}</div>`;
            if (item.type && item.type.length) content += `<div class="section-title">Content Type</div><div class="detail-tags">${item.type.map(t => `<span class="detail-tag type-tag">${t}</span>`).join('')}</div>`;
            if (item.details) content += `<div class="section-title">Details</div><div class="section-content">${item.details}</div>`;
            if (item.implementationNotes) content += `<div class="section-title">Implementation Notes</div><div class="section-content">${item.implementationNotes}</div>`;
            if (item.primaryUsers) content += `<div class="section-title">Primary Users</div><div class="section-content">${item.primaryUsers}</div>`;
            if (item.aiIntegrationPriority) content += `<div class="section-title">AI Integration Workflow</div><div class="section-content" style="color: ${isHighAIPriority(item.aiIntegrationPriority) ? '#0369a1' : 'inherit'}; font-weight: ${isHighAIPriority(item.aiIntegrationPriority) ? '600' : 'normal'};">${item.aiIntegrationPriority}</div>`;
            const challenges = extractChallenges(item);
            content += `<div class="section-title">Potential Implementation Challenges</div><div class="section-content"><ul>${challenges.map(c => `<li>${c}</li>`).join('')}</ul></div>`;
            detailContentEl.innerHTML = content;
            detailPanelEl.style.display = 'block';
            overlayEl.style.display = 'block';
        }

        function filterItems() {
            const searchText = searchInput.value.toLowerCase();
            const cat = categoryFilter.value; // Renamed to avoid conflict
            const sys = systemFilter.value; // Renamed to avoid conflict
            const typ = typeFilter.value; // Renamed to avoid conflict
            const priv = privacyFilter.value; // Renamed to avoid conflict
            const stru = structureFilter.value; // Renamed to avoid conflict
            const ai = aiFilter.value;

            const filtered = knowledgeItems.filter(item => {
                const itemLabel = (item.label || '').toLowerCase();
                const itemDetails = (item.details || '').toLowerCase();
                const itemId = (item.id || '').toLowerCase();
                if (searchText && !itemLabel.includes(searchText) && !itemDetails.includes(searchText) && !itemId.includes(searchText)) return false;
                if (cat && item.category !== cat) return false;
                if (sys && !item.systems.includes(sys)) return false;
                if (typ && (!item.type || !item.type.includes(typ))) return false;
                if (priv && item.privacy !== priv) return false;
                if (stru && item.structure !== stru) return false;
                if (ai) {
                    const itemAIPriority = (item.aiIntegrationPriority || '').toLowerCase();
                    if (ai === 'High' && !isHighAIPriority(item.aiIntegrationPriority)) return false;
                    if (ai === 'Medium' && !(itemAIPriority.includes('medium') || (!itemAIPriority.includes('high') && !itemAIPriority.includes('low') && !isHighAIPriority(item.aiIntegrationPriority)))) return false;
                    if (ai === 'Low' && !itemAIPriority.includes('low')) return false;
                }
                return true;
            });
            renderKnowledgeMap(filtered);
        }

        // Initial Render
        renderKnowledgeMap(knowledgeItems);
    </script>
</body>

</html>
